import { CompletionStatus } from './enum/CompletionStatus';

export type EducationType = {
  school: string;
  degree: string;
  iconSrc: string;
  timeline: string;
  website: string;
  labels: string[];
};
